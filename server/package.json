{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "seed:developer": "ts-node --transpile-only src/scripts/seedDeveloper.ts",
    "stripe:setup": "ts-node --transpile-only src/scripts/stripeSetup.ts",
    "build": "prisma generate && tsc",
    "start": "prisma migrate deploy && node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/multer": "^2.1.0",
    "@types/node": "^24.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "date-fns": "^4.4.0",
    "dotenv": "^17.4.2",
    "express": "^5.1.0",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "prisma": "^6.15.0",
    "react-big-calendar": "^1.20.0",
    "resend": "^6.14.0",
    "stripe": "^22.2.2",
    "typescript": "^5.6.0"
  },
  "//": "typescript and the @types/* used by `npm run build` (tsc) live in dependencies, not devDependencies, on purpose: Render builds from source with NODE_ENV=production, under which `npm install` omits devDependencies. Keeping the build toolchain in dependencies makes the production build work regardless of NODE_ENV. ts-node-dev and @types/react-big-calendar stay dev-only (not needed by the prod build).",
  "devDependencies": {
    "@types/react-big-calendar": "^1.16.3",
    "ts-node-dev": "^2.0.0"
  }
}
