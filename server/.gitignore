node_modules
dist

# Keep environment variables out of version control
.env

/src/generated/prisma

# User-uploaded project attachments (real files, see middleware/upload.middleware.ts)
/uploads

.env.local
tsconfig.tsbuildinfo

# Rendered email previews (npm run emails:preview) — build output, not source.
/.email-preview
prisma/axeriva.db

# Legacy: the integration suite used a throwaway SQLite file before the
# PostgreSQL migration. It now uses a disposable PostgreSQL database
# (TEST_DATABASE_URL), so nothing is written here — kept so an old working
# copy's leftover file can never be committed.
prisma/test.db
prisma/test.db-journal
